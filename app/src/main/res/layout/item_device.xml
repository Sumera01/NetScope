<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp"
    android:layout_marginBottom="8dp"
    android:background="@drawable/device_item_bg">

    <TextView
        android:id="@+id/deviceVendor"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Device Hostname"
        android:textColor="@color/teal_accent"
        android:textStyle="bold"
        android:textSize="16sp"/>

    <TextView
        android:id="@+id/deviceIP"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="IP: 192.168.1.105"
        android:textColor="@color/white_text"
        android:layout_marginTop="2dp"/>

    <TextView
        android:id="@+id/deviceMAC"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="MAC: Hidden by Android"
        android:textColor="@color/gray_text"
        android:layout_marginTop="2dp"/>

    <!-- NEW: Open Ports -->
    <TextView
        android:id="@+id/devicePorts"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ports: 80, 443"
        android:textColor="@color/teal_accent"
        android:textStyle="italic"
        android:layout_marginTop="4dp"/>
</LinearLayout>